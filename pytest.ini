[pytest]
testpaths = functional regression
markers =
    smoke: quick smoke tests (< 2 min)
    standard: standard integration tests (< 10 min)
    full: full tests including pjdfstest (< 60 min)
addopts = -v --tb=short
